---
const { piece } = Astro.props;
---
<a class="piece-card" href={`/pieces/${piece.slug}`} data-modal>
  <span class="tag-pill">{piece.tag}</span>
  <h3>{piece.title}</h3>
  <p class="is">{piece.is}</p>
  <p class="demo"><b>Demonstrates:</b> {piece.demonstrates}</p>
</a>
